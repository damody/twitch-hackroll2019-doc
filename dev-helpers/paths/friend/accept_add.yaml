post:
  summary: accept add room 接受加朋友
  tags:
    - friend
  parameters:
  requestBody:
    required: true
    content:
      application/json:
        schema:
          # Body schema with atomic property examples
          type: object
          required:
            - userid
          properties:
            userid:
              type: string
              example: 'damody'
              description: |
                user id
                使用者代號
  responses:
    '200':
      description: |
        Success
      headers:
    '500':
      description: unexpected error
      content:
        application/json:
          schema:
            $ref: "/schemas/common.yaml#Error"
