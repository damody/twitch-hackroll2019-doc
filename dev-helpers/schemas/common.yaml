Success:
  required:
    - error
  properties:
    msg:
      type: string
      example: ok
      description: real error message in there.
Error:
  required:
    - error
  properties:
    msg:
      type: string
      example: fail.
      description: real error message in there.